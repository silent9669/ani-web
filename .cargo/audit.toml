[advisories]
# These advisories are currently pulled through Tauri's Linux GTK/WebKit stack.
# Keep cargo-audit enabled so new direct or fixable advisories still fail CI.
ignore = [
  "RUSTSEC-2024-0429", # glib 0.18.x VariantStrIter unsoundness via Linux GTK bindings
]
